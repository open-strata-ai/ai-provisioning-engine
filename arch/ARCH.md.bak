# ai-provisioning-engine · 架构文档

> 从 design/DESIGN.md 浓缩。来源：§1 定位与边界、§2 职责清单、§3 核心抽象与接口、§6 外部适配。
> 平台版本 v1.4.0 | Go + Gin + Cobra + Wire | assembly 领域

---

## 1. 组件定位

`ai-provisioning-engine` 是 OpenStrata 装配中枢的**后半程**：部署/供给引擎。它消费 `ai-dependency-resolver` 产出的 AssemblyPlan，把计划落地为 **Helm Values / Compose / Operator 配置**，并通过 **ArgoCD 或直连 Helm** 应用到运行环境，落实「增量部署、滚动更新、零停机」。

### 核心问题
把"一份确定的装配计划"变成"对环境的安全、可观测、可回滚的实际变更"——只动差异部分，已运行服务不重启。

### 必选性
core。阶段一~三由 `aictl up` 驱动 Compose；full 档由引导门户 + ArgoCD GitOps 驱动。

### 边界划分

| 组件 | 关系 | 说明 |
|------|------|------|
| **ai-dependency-resolver** | 上游 | Resolver 产出 Plan -> 本仓消费 Plan，只执行、不算依赖 |
| **ai-cli** | 调用方 | `aictl up/apply` 透传调用本仓内核 |
| **ai-guide-portal** | 调用方 | 门户触发执行编排，本仓是执行内核 |
| **被部署组件** | 部署对象 | 本仓是 gateway/sandbox 等组件的"装配者" |

---

## 2. 职责清单

| # | 职责 | 类型 | 说明 |
|---|------|------|------|
| R1 | Plan 消费与校验 | core | 读取 AssemblyPlan，预检资源/依赖 |
| R2 | 渲染配置 | core | Plan -> Helm Values / Compose / Operator |
| R3 | 增量部署 | core | 仅部署/变更差异 |
| R4 | 滚动更新 | core | 多副本滚动 + 探针保活 |
| R5 | 灰度升级 | core | 影响面大者双写校验后切 |
| R6 | 回滚 | core | 声明式回滚（enabled=false 重放） |
| R7 | 状态回传 | core | 组件 Ready 状态回传门户 |

---

## 3. 核心抽象与接口

### 3.1 执行模型

```go
// 渲染产物
type RenderOutput struct {
    Kind      string            // helm-values | compose | k8s-manifest
    Artifacts map[string][]byte // 文件名 -> YAML
}

// 执行结果
type ApplyResult struct {
    Component string // 组件名
    Action    string // add|reuse|remove|rolling-update
    Status    string // success|failed|in-progress
    Message   string
}

// 部署目标 Port（SPI: CICD 1.0.0）
type Deployer interface {
    Render(ctx context.Context, plan AssemblyPlan, profile string) (RenderOutput, error)
    Apply(ctx context.Context, out RenderOutput) ([]ApplyResult, error)
    Rollback(ctx context.Context, component string, toVersion string) error
    Status(ctx context.Context, component string) ComponentStatus
}

type ComponentStatus struct {
    Name     string
    Ready    bool
    Version  string
    Replicas int
}

// CICD SPI（interface_versions.CICD = 1.0.0）
type CICDPort interface {
    Sync(ctx context.Context, manifest []byte) error
    RollbackTo(ctx context.Context, revision string) error
}
```

### 3.2 分层架构

| 层 | 包 | 职责 |
|----|-----|------|
| 领域层 | `domain/` | Deployer Port + RenderOutput/ApplyResult 类型 |
| 应用层 | `app/` | RenderAndApply / RollbackComponent 用例 |
| 适配器层 | `adapter/` | HelmAdapter / ComposeAdapter / ArgoCDAdapter |
| 基础设施层 | `infrastructure/` | 配置、Wire DI、K8s/Helm 客户端 |

---

## 4. 处理流水线

```
门户/aictl -> ai-provisioning-engine
  -> 预检: 资源配额 + 依赖图 + 冲突
  -> [失败] 阻断 + 原因
  -> [通过] 按 profile 渲染（Helm Values/Compose/Operator）
  -> 差量应用: 仅 Added/Removed/变更
  -> [多副本] 滚动更新（maxSurge/maxUnavailable + 探针）
  -> [影响面大] 双写校验后切（灰度）
  -> [移除] 声明式下线（enabled=false）
  -> 探测 Ready
  -> [未 Ready] 重试/超时 -> 回滚
  -> [Ready] 状态回传门户
  -> 完成，原有服务零停机
```

---

## 5. 外部适配器（SPI Adapter）

| SPI 端口 | 本仓角色 | 外部组件 | 默认/备选 | Adapter |
|----------|----------|----------|-----------|---------|
| `CICD` (1.0.0) | 消费方 | ArgoCD（optional）+ Istio（optional，仅 full） | 备选/备选 | ArgoCDAdapter（GitOps sync/rollback） |
| `Cache` (1.0.0) | 消费方 | Redis（core） | 默认 | 执行状态/锁 |
| `Tracing` (1.0.0) | 消费方 | OTel（core） | 默认 | 部署链路 trace |
| 部署目标 | 直接驱动 | Kubernetes（Helm/Kubectl）/ Docker Compose | 默认 | HelmAdapter / ComposeAdapter |

### 阶段适配
- CICD（ArgoCD/Istio）为 optional（仅 full 档）
- starter/standard/advanced 走直连 Helm/Compose
- full 档走 ArgoCD（SPI 多实现并存）
- 所有部署目标经 Adapter 隔离，切换零改动

---

## 6. 关键设计决策

| 决策 | 内容 |
|------|------|
| 增量原则 | 仅对 Plan.Added 部署、Plan.Removed 下线、Plan.Reused 跳过 |
| 渲染策略 | 按 profile 选渲染器：starter=Compose；standard+/advanced/full=Helm/K8s |
| 滚动策略 | maxSurge:1 / maxUnavailable:0 + 存活/就绪探针 |
| 灰度升级 | 双写校验后再切（如切向量库 Milvus<->Qdrant） |
| 回滚 | 声明式：enabled 改回 false/旧版本重放 Plan，ArgoCD/GitOps 自动回滚 |
| 部署命名空间 | `ai-system`（core） |
